# IMPORTANTE: Ponemos una URL absoluta para que no falle el JSON
baseURL = "https://energyflash-powertrader.netlify.app"
languageCode = "es-es"
title = "PowerTrader EnergyFlash"
theme = "PaperMod"

[params]
    env = "production"
    title = "PowerTrader EnergyFlash"
    description = "Inteligencia diaria de mercados"
    author = "PowerTrader"

    # Configuración mínima para que el tema no se queje
    [params.assets]
        disableHLJS = true
